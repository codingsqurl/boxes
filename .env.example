# Server
PORT=3000
NODE_ENV=production

# Your public domain — locks CORS to only this origin in production
SITE_URL=https://treehopperscos.com

# SQLite database path
DB_PATH=./data/tree-hoppers.db

# Blog image upload directory
UPLOAD_DIR=./data/uploads

# Business contact info — used in email templates
CONTACT_EMAIL=treehopperscos@gmail.com
CONTACT_PHONE=(719) 287-8836

# Gmail SMTP — use an App Password, NOT your account password
# Generate at: https://myaccount.google.com/apppasswords
# (Requires 2FA to be enabled on the Google account)
GMAIL_USER=treehopperscos@gmail.com
GMAIL_APP_PASSWORD=xxxx xxxx xxxx xxxx

# Admin login — run `npm run set-dev codingsqurl "YourPassword"` to create/update the dev account
# Or set these and the account is auto-seeded on first boot
ADMIN_USERNAME=codingsqurl
ADMIN_PASSWORD=your-password-here
# Internal API keys — both random, users never see these
# Generate each with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_API_KEY=your-generated-hex-key-here
DEVELOPER_API_KEY=your-other-generated-hex-key-here

# Twilio SMS — texts Paul when a new lead or appointment comes in
# Sign up at: https://www.twilio.com
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your_auth_token_here
TWILIO_FROM=+17195550000
TWILIO_TO=+17192878836

# Google APIs — both optional, site works without them
# Maps API key: https://console.cloud.google.com (enable Maps JavaScript API + Places API)
GOOGLE_MAPS_API_KEY=your_key_here
# Place ID for Tree Hoppers Google listing (find at: https://developers.google.com/maps/documentation/javascript/examples/places-placeid-finder)
GOOGLE_PLACE_ID=ChIJxxxxxxxxxxxxxxxxxxxxxxxxx
